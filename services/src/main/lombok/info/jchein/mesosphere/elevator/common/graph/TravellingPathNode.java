package info.jchein.mesosphere.elevator.common.graph;

/**
 * A polymorphism root for the three nodes that represent a group of passengers in an elevator car along a single hop of an elevator car's path, and a pair
 * of pseudonodes representing global states before reaching the elevators and after leaving them behind.
 * 
 * Each value of the TravelPathNodeType correlates to one of the five classes defined above.  This is a closed hierarchy.
 * 
 * @author jheinnic
 *
 */
public interface TravellingPathNode
{
//   public TravelPathNodeType getPathNodeType();
}
